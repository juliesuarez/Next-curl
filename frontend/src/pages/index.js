import React from 'react';
import Header from '../components/Header';

const Home = () => (
  <div>
    <Header />
    <main>
      <h1>Welcome to Next-curl</h1>
      <p>This is the homepage of the Next-curl educational platform.</p>
    </main>
  </div>
);

export default Home;
